#class linked list
class DoubleList(object)
    head = None
    tail = None
    #menambah node
    def menuTambah(self):
        temp = Node(input('masuka data baru : '))
        new_node = Node(temp, None, Node,)
        if self.head is None: #mengecek apakah list kosong
            self.head = self.tail = new_node
        else: #jika tidak kosong
            new_new.prev = self.tail
            new_node.next = None
            self.tail.next = new_node
            self.tail = new_node
#menambahkn isi text
def menuTambah(self):
    temp = input('masukan data baru : ')
    new_node = Node(temp, None, Node,)
    if self.head is None: #mengecek apakah list kosong
        self.head = self.tail = new_node
    else: #jika tidak kosong
        new_new.prev = self.tail
        new_node.next = None
        self.tail.next = new_node
        self.tail = new_node

#mengapus node
def MenuHapus(self):
    temp = int(input('masukan data yang mau di hapus'))
    #membut pointer yang merujuk node pertama
    current_node = self.head
    #melakukan perulangan saat list tidak kosong
    while current_node is not None:
        #memeriksa data yang akan di hapus
        if current_node.data == temp:
            #jika node yang di cari berada di elemen terakhir
            if current_node.data == temp:
                if current_node.next is None:
                    current_node.next = None
            #jika node yang di cari berada bukan pada elemen pertama
            elif current_node.prev.next = current
            current_node.prev.next = current_node.next
            current_node.next.prev = current_node.prev
            return
        current_node = current_node.next
    print('data tidak ditemukan')
#mencari data
def MenuCari(self):
    temp = int(input('masukan data yang mau di cari'))
    current_node = self.head
    while current_node is not None:
        if current_node.data == temp:
            return True
        current_node = current_node.next
    return False
#menampilkan data
def MenuTampil(self):
    current_node = self.head
    while current_node is not None:
        print(current_node.data)
        current_node = current_node.next
#menampilkan data terbalik
def MenuTerbalik(self):
    current_node = self.tail
    while current_node is not None:
        print(current_node.data)
        current_node = current_node.prev
#menampilkan data dengan cara mengulang
def MenuCara(self):
    current_node = self.head
    while current_node is not None:
        print(current_node.data)
        current_node = current_node.next
#menghapus semua data
def MenuHapusSemua(self):
    self.head = None
    self.tail = None
#menghapus data tertentu
def MenuHapusTertentu(self):
    temp = int(input('masukan data yang mau di hapus'))
    current_node = self.head
    while current_node is not None:
        if current_node.data == temp:
            if current_node.data == temp:
                if current_node.next is None:
                    current_node.next = None
            elif current_node.prev.next = current
            current_node.prev.next = current_node.next
            current_node.next.prev = current_node.prev
            return
        current_node = current_node.next
    print('data tidak ditemukan')
#mencari data
def MenuCari(self):
    temp = int(input('masukan data yang mau di cari'))
    current_node = self.head
    while current_node is not None:
        if current_node.data == temp:
            return True
        current_node = current_node.next
    return False
#menampilkan data
def MenuTampil(self):
    current_node = self.head
    while current_node is not None:
        print(current_node.data)
        current_node = current_node.next
#menampilkan data terbalik
def MenuTerbalik(self):
    current_node = self.tail
    while current_node is not None:
        print(current_node.data)
        current_node = current_node.prev
#menampilkan data dengan cara mengulang
def MenuCara(self):
    current_node = self.head
    while current_node is not None:
        print(current_node.data)
        current_node = current_node.next
#menghapus semua data
def MenuHapusSemua(self):
    self.head = None
    self.tail = None
#menghapus data tertentu
def MenuHapusTertentu(self):
    temp = int(input('masukan data yang mau di hapus'))
    current_node = self.head
    while current_node is not None:
        if current_node.data == temp:
            if current_node.data == temp:
                if current_node.next is None:
                    current_node.next = None
            elif current_node.prev.next = current
            current_node.prev.next = current_node.next
            current_node.next.prev = current_node.prev
            return
        current_node = current_node.next
    print('data tidak ditemukan')
#mencari data
def MenuCari(self):
    temp = int(input('masukan data yang mau di cari'))
    current_node = self.head
    while current_node is not None:
        if current_node.data == temp:
            return True
        current_node = current_node.next
    return False
#menampilkan data
def MenuTampil(self):
    current_node = self.head
    while current_node is not None:
        print(current_node.data)
        current_node = current_node.next
#menampilkan data terbalik
def MenuTerbalik(self):
    current_node = self.tail
    while current_node is not None:
        print(current_node.data)
        current_node = current_node.prev
#menampilkan data dengan cara mengulang
def MenuCara(self):
    current_node = self.head
    while current_node is not None:
        print(current_node.data)
        current_node = current_node.next
#menghapus semua data
def MenuHapusSemua(self):
    self.head = None
    self.tail = None
#menghapus data tertentu
def MenuHapusTertentu(self):
    temp = int(input('masukan data yang mau di hapus'))
    current_node = self.head
    while current_node is not None:
        if current_node.data == temp:
            if current_node.data == temp:
                if current_node.next is None:
                    current_node.next = None
            elif current_node.prev.next = current
            current_node.prev.next = current_node.next
            current_node.next.prev = current_node.prev
            return
        current_node = current_node.next
    print('data tidak ditemukan')
#mencari data
def MenuCari(self):
    temp = int(input('masukan data yang mau di cari'))
    current_node = self.head
    while current_node is not None:
        if current_node.data == temp:
            return True
        current_node = current_node.next
    return False
#menampilkan data
def MenuTampil(self):
    current_node = self.head
    while current_node is not None:
        print(current_node.data)
        current_node = current_node.next
#menampilkan data terbalik
def MenuTerbalik(self):
    current_node = self.tail
    while current_node is not None:
        print(current_node.data)
        current_node = current_node.prev
#menampilkan data dengan cara mengulang
